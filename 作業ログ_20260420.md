# 作業ログ — 2026年4月20日

---

## 1. Career Path カード — 飾り文字・バッジの改修

- **バッジをピル型に変更**：`cs-year-badge` を `position:absolute` から通常フローに戻し、水色枠・水色背景のピル型バッジに変更（`border:1.5px solid var(--sky-200)`）
- **飾り文字を追加**：`cs-year-deco` クラスを新設。Homemade Apple フォント 30px、水色、カード右上角を基点にカードからはみ出す配置（ペンで落書きした印象）
- **テキスト内容**：Year 1 / Year 2-3 / Year 4-5 / Year 6-8 / Year 9+（全5枚）
- **overflow修正**：`cs-card` の `overflow:hidden` を削除し、`.cs-photo` に `border-radius` と `overflow:hidden` を移動して画像の角丸を維持しつつ飾り文字のはみ出しを許可

---

## 2. 飾り文字の角度調整

- `rotate(3deg)` → `rotate(30deg)` → `rotate(15deg)` に調整（ユーザー指示により最終 15deg）

---

## 3. 飾り文字の位置調整

- `top:-14px` → `top:6px`（20px 下に移動）

---

## 4. 福利厚生カードのスタイル改修

- **背景色**：`var(--cream-50)` → `var(--white)`（白）
- **枠線**：`var(--cream-200)` → `var(--sky-200)`（水色、細め）
- **区切り線**：`var(--cream-300)` → `var(--sky-200)`（水色）
- **影**：チェキと同系の `box-shadow:2px 4px 16px rgba(0,0,0,.10)` を追加

---

## 5. 募集要項セクションの改修

- **バッジ削除**：`<span class="sec-eyebrow">募集要項</span>` を削除
- **ホバーカラー削除**：`.req-table tr:hover` のスタイルを削除
- **CTA追加**：各タブ（施工管理・住宅設計・賃貸管理）の表の下に「エントリーはこちら」ボタン（`btn-hero-main` スタイル）を設置

---

## 6. Career Path カード間の矢印を SVG に変更

- **▼ 三角を SVG 矢印に置き換え**：`yazirusi2.svg` → `yazirusi3.svg`（`viewBox="0 0 17.03 28.65"`）
- **塗りに変更**：`stroke` → `fill:#6bbde8`（水色の塗り）、スクロール時にフェードイン表示
- **IntersectionObserver でアニメーショントリガー**を追加

---

## 7. TOPビューの統計情報を削除

- `<div class="hero-stats">` ブロック（15年/創業年数・1,200棟/累計施工数・94%/顧客満足度）を削除

---

## 8. TOPビューの「●」ドットを削除

- `<span class="hero-tag-dot"></span>` を削除（「New Graduate Recruitment 2027」左の点）

---

## 9. ヘッダーナビのリンク先変更

- 「仕事を知る」の飛び先：`#work` → `#dayflow`（1日の仕事の流れ）
- ドロワーメニューも同様に変更

---

## 10. TOPビューのボタン変更

- 「仕事を知る」→「会社を知る」に名称変更
- 飛び先：`#work` → `about.html`

---

## 11. TOPビューのメインコピーをテキストに置き換え

- **フォント追加**：`Zen Old Mincho` を Google Fonts に追加
- **画像をコメントアウト**：`<h1 class="hero-h1">` 内の base64 画像を `<!-- -->` でコメントアウト（復元可能）
- **テキストコピーを設置**：
  - 1行目：`技術を磨き、デザインで挑む。`
  - 2行目：`安定を手に、キャリアを築く。`
- **ハイライト**：「技術」「デザイン」「安定」「キャリア」を水色（`var(--sky-300)`）に
- **枠スタイル**：半透明背景＋白枠線、Zen Old Mincho フォント（参考：monexgroup.jp/recruit）

---

## 変更ファイル

| ファイル | 内容 |
|---|---|
| `recruit_2.html` | 上記すべての変更を適用 |
| `component-library/index.html` | ※今回セッションでの変更なし |
